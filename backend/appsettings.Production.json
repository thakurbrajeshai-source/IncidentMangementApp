{
  "// Production template. In real prod, provide a strong Jwt:Key (32+ chars) via env var and the real SQL Server connection string.",
  "Database": {
    "Provider": "SqlServer"
  },
  "Auth": {
    "UseTestOtp": false
  }
}
