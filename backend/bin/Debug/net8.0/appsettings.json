{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "// Database": "Provider = 'Sqlite' (dev) or 'SqlServer' (production). Connection string lookup is per-provider.",
  "Database": {
    "Provider": "Sqlite"
  },
  "ConnectionStrings": {
    "Sqlite": "Data Source=incident_management.db",
    "// SqlServer": "Production. Set Database:Provider=SqlServer and provide the real connection string in appsettings.Production.json or via env var.",
    "SqlServer": "Server=localhost;Database=IncidentManagement;Trusted_Connection=True;TrustServerCertificate=True;"
  },

  "// Auth": "UseTestOtp=true issues '123456' for every login. Set to false only after wiring a real provider (see TODO in Program.cs).",
  "Auth": {
    "UseTestOtp": true,
    "TestOtp": "123456",
    "OtpTtlMinutes": 10
  },

  "Jwt": {
    "Issuer": "IncidentManagement.Api",
    "Audience": "IncidentManagement.Client",
    "// PRODUCTION": "REPLACE this key with a cryptographically random 32+ char string. Use: openssl rand -base64 32. Set via env var Jwt:Key in production — NEVER commit a real secret to source control.",
    "Key": "DEV-ONLY-REPLACE-ME-WITH-32+-CHAR-RANDOM-SECRET-IN-PRODUCTION-xxxxxxxx",
    "AccessTokenMinutes": 720
  },

  "Incident": {
    "AutoCloseAfterHours": 48,
    "AutoCloseCheckMinutes": 10
  },

  "// Workflow": "AuthConfigEncryptionKey encrypts workflow step credentials at rest (AES-256 via AuthConfigProtector). Change + re-save workflows together in production.",
  "Workflow": {
    "AuthConfigEncryptionKey": "DEV-ONLY-workflow-auth-config-key-change-in-production"
  }
}
